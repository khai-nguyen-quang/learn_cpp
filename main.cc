#include <bits/stdc++.h>
#include <thread>
#include <string>

#include "hello/hello.h"


// extern void main_hashtable();
// extern void main_concurrency();
extern void main_threadpool();
using namespace std;

int main () {
    // std::cout << say_hello() << std::endl;
    // cout <<  "number of hardware threads: " << thread::hardware_concurrency() << endl;
    // main_hashtable();
    // main_concurrency();
    main_threadpool();
}

